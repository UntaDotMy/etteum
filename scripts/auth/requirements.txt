# Browser automation: nodriver (default engine).
# Only the `antigravity` provider is migrated to nodriver so far; the other
# browser providers are disabled (see app.providers.browser_utils.raise_browser_unavailable).
nodriver>=0.50
aiohttp==3.13.3
aiohttp-socks==0.10.1
httpx==0.28.1
cbor2>=5.6.0
pydantic==2.12.5
pydantic-settings>=2.0.0,<3.0.0
curl_cffi>=0.9.0
