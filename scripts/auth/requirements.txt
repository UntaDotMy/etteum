camoufox[geoip]==0.4.11
playwright==1.58.0
python-dotenv>=1.0.0
aiohttp==3.13.3
aiohttp-socks==0.10.1
httpx==0.28.1
browserforge>=1.0.0
# canva_worker.py TLS impersonation (runtime uses same auth venv as camoufox_flow)
curl_cffi>=0.7.0
# Optional: SOCKS for Grok farm CLI probe / token exchange (browser already supports SOCKS)
# PySocks>=1.7.1
